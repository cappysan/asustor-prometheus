# Functions
# =========
function logger() {
  echo "${@}" >&2
  syslog --log 0 --level 0 --user SYSTEM --event "${@}"
}

WHAT=Prometheus

# User
# ====
export APKG_USER=prometheus
export APKG_GROUP=root
export HOME=/share/Configuration/prometheus

# vim: ft=sh
